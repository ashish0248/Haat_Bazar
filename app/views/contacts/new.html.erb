<div class="body">
    <div class="container">
      <div class="row">
       	<h3><お問い合わせフォーム</h3>
       	<div>
       		<%= form_for(@contact_new) do |f| %>
         		<%= f.text_area :message %>
         		<%= f.submit '送信' %>
            <% end %>
       	</div>
      </div>
    </div>
</div>